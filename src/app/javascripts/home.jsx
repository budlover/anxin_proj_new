const React = require('react');
const ReactDOM = require('react-dom');
const Table = require('./../../shared/components/table');

ReactDOM.render(
    <Table />,
    document.getElementById('root')
);
